# Imagens da landing page

Adicione aqui os arquivos de imagem usados no `index.html` com estes nomes:

- `drink-destaque.jpg`
- `drink-rosa.jpg`
- `drink-laranja.jpg`

> Observação: atualmente os `.jpg` neste diretório estão vazios (2 bytes) e por isso não carregam no navegador.
> Enquanto isso, o HTML usa fallback automático para arquivos `.svg` com o mesmo nome-base.
